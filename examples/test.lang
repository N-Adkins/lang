var is_zero := fn(x: number) -> void {
    if (x == 0) {
        print("is zero");
    } else {
        print("is not zero");
    }
    return;
};

is_zero(231);
is_zero(0);
is_zero(2138);
is_zero(0);
