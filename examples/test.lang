var fib := fn (x: number) -> number {
    var state := [0, 1];
    var i := 0;
    while (i < x) {
        state[i % 2] = state[0] + state[1];
        i = i + 1;
    }
    return state[i % 2];
};

print(fib(30));
